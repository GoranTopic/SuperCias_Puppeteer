/* this script initlizes the necesary value to start scrapping */
import { makeFileStorage } from '../init.js';
import Storage from 'dstore-js';

let make_fileStore = makeFileStorage

export default make_fileStore;

